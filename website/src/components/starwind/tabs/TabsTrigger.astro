---
import type { HTMLAttributes } from "astro/types";
import { tv } from "tailwind-variants";

interface Props extends Omit<HTMLAttributes<"button">, "type" | "id" | "role"> {
  value: string;
}

export const tabsTrigger = tv({
  base: [
    "inline-flex items-center justify-center gap-2 rounded-full border border-transparent px-3 py-1.5 font-medium whitespace-nowrap transition-[background-color]",
    "border-border text-muted-foreground",
    "data-[state=active]:bg-foreground data-[state=active]:text-background",
    "hover:bg-muted hover:text-foreground hover:data-[state=active]:bg-primary hover:data-[state=active]:text-background",
    "[&_svg]:pointer-events-none [&_svg]:shrink-0 [&_svg:not([class*='size-'])]:size-4",
    "focus-visible:border-outline focus-visible:ring-outline/50 focus-visible:outline-outline focus-visible:ring-3 focus-visible:outline-1",
    "disabled:pointer-events-none disabled:opacity-50",
  ],
});

const { value, class: className, ...rest } = Astro.props;
---

<button
  class={tabsTrigger({ class: className })}
  data-slot="tabs-trigger"
  data-tabs-trigger
  data-value={value}
  data-state="inactive"
  role="tab"
  aria-selected="false"
  {...rest}
>
  <slot />
</button>
